# mtcarstools

**Elevator pitch:**  
A beginner-friendly toolkit for exploring the mtcars dataset.

## Core functions

- `summarize_mtcars(by = NULL)`
- `scatter_mtcars(x, y, color = NULL)`
- `corr_plot_mtcars(vars = NULL)`
- `fit_mpg_model(predictors = NULL)`
